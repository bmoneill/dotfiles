#!/bin/sh

[ -z "$1" ] || sr "stackoverflow.com/search?q=" "$1" >/dev/null
[ -z "$1" ] || exit

term=$(echo | dmenu -p "stackoverflow:")
[ -z "$term" ] || sr "stackoverflow.com/search?q=" "$term" >/dev/null
