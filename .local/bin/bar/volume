#!/bin/sh

vol="$(pulsemixer --get-volume | cut -d ' ' -f 1)"

echo "$vol" | grep "\[off\]" >/dev/null && printf "🔇\\n" && exit

if [ "$vol" -gt "70" ]; then
	icon="🔊"
elif [ "$vol" -lt "30" ]; then
	icon="🔈"
else
	icon="🔉"
fi

printf "%s %s%%\\n" "$icon" "$vol"
